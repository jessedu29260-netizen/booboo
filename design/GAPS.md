# GAPS — what is actually true, right now

> The specs (`CRAFT.md`, `examples/pemberton/DESIGN.md`) say what we INTEND.
> This file says what EXISTS, verified by looking at the live thing. It is the
> only doc that may claim "done".
>
> **Law:** no surface gets called finished anywhere — commit message, PR body,
> chat — unless it has a row here marked ✅ with the evidence that proved it.
> Update this file in the same commit as the work. A gap discovered and not
> written here is a gap that will be rediscovered later at higher cost.
>
> Live: https://booboo.fractionalhq.uk · Audited 2026-07-18 · updated 2026-07-19.
>
> **Standing verdict (Jesse, 2026-07-19):** readable now, but it "doesn't get
> out of the mould" — competent dark-SaaS-with-brass, not a category of one.
> The open work is a craft pass with a real point of view, not more features.
>
> **Craft pass shipped, same day.** The hospitality-materiality direction
> (HANDOFF.md) is built and live on both faces — a brass staff board with a
> traceable rule of law, and a cosmos where authority literally falls as
> light. This is a claim to be re-judged, not a close: the next open of
> booboo.fractionalhq.uk is the test, not this file.

> **Where this sits:** `design/GOALS.md` (added 2026-07-19) states the six goals
> and how far each is. This file stays the record of what is TRUE, with evidence.
> Goals aim; GAPS reports. When they disagree, GAPS wins.

## Legend

✅ done + verified · 🟡 exists, incomplete · 🔴 missing · ⬜ not started

---

## A · SPEC vs SHIPPED — every commitment in CRAFT.md, checked in code

The first version of this file audited the visitor journey and **not the spec**,
which is how a whole slice went missing unnoticed. Checked by grepping the
source, not by memory.

### A0 · Tokens (CRAFT §0 — "single source, three consumers")

| Commitment | State | Reality |
|---|---|---|
| `tokens.json` consumed by site | ✅ | `web/tokens.css` generated by `scripts/gen-tokens.mjs`, linked before `styles.css`; the hand-written `:root` palette is gone. Verified live: `/tokens.css` 200, site renders unchanged. |
| …by viewer | ✅ | `packages/viewer/src/tokens.ts` generated; `layout.ts` re-exports FLAG/VERB palettes from it instead of the hand-mirrored copies. |
| …by panel | 🟡 | `panel/app/tokens.css` now generated alongside; `panel.css` keeps its own semantic aliases over those raw values (deliberate — the panel names things `--ink`/`--accent`). |

Rule zero holds now: `design/tokens.json` is the one edit, and
`scripts/gen-tokens.mjs` runs inside `build-web.mjs` so a stale build cannot
ship a drifted palette.

### A1 · The luminance ladder (CRAFT §1) — the top three ranks are missing

Declared order: `flags > badges > pulses > landmarks > field > edges > discs > bg`.

| Rank | State | Reality |
|---|---|---|
| flags | ✅ | Ringed beacons, ranked colours, depthTest off so they never hide behind geometry, ignite last in the entrance, clickable → dossier. Health amber/red raises a flag too. Verified live: 5 beacons (4 explicit + Engineering amber), red critical reads first. |
| badges/contracts | 🟡 | Landmarks are brass objects ✅ but carry no icon, no name plate, no contract distinction. |
| pulses | 🟡 | Edges pulse, but uniformly — no direction, no meaning. |
| landmarks / field / discs / bg | ✅ | Built and verified. |

**This is the forgotten slice.** The half of the two-second test that asks
*"where is the problem"* has no implementation at all. Engineering runs amber
in the data and looks identical to every other department on screen.

### A2 · Scene (CRAFT §2)

| Commitment | State |
|---|---|
| Landmarks as brass objects + contact shadows | ✅ |
| Observatory floors: glass, etched rules, engraved names | ✅ |
| Sprite field: soft core, rim, depth fade | ✅ |
| **Light-shaft spines** (cone beams, *authority as light falling*) | ✅ **shipped 2026-07-19** — one merged baked mesh (matches the Field/PulseLinks single-draw-call pattern), a spotlight-cone frustum per structural parent→child pair (tier≤2 only — GM→heads→SOPs→named staff, the same edge-culling scope used everywhere else), narrow at the source and widening as it falls, fresnel-lit silhouette, gradient alpha, animated downward-drifting grain, top-down entrance ignition sharing the same clock as the rest of the intro sequence. De-bloomed by default (NormalBlending when `cfg.bloom` is 0, matching Field/PulseLinks — the one place I first got this wrong, see the landmine below). Verified live on `/viewer/` and embedded on `/` behind the hero headline: `viewer-fixed2.png`, `preset-departments.png`, `landing-check.png` (headless GPU screenshots, real render). |
| Edges: fat ribbons, source→target gradient, directional dashes | 🔴 thin GL lines |
| **Verb → token colour on edges** | ✅ VERB_COLOR default when a link has no explicit colour, + a source→target brightness gradient so direction reads in a still frame. Verified live: reports_to gold, owns teal, escalates_to red, audits violet all distinct. |
| Atmosphere: height fog, nebula IBL | 🟡 starfield only |
| Band rim-rings | ✅ **removed by default 2026-07-19** (Jesse: "take out those big rings, not needed here"). The rim torus drew a hard bright ellipse round every band — after the flags, the brightest thing in frame, for pure decoration, which inverts the CRAFT §1 luminance ladder. The engraved floor already names the band. Kept as a `rings` flag (BoobooCfg is published API) but defaulted false; `persistKey` bumped v3→v4 so returning visitors get the new default instead of a stale localStorage copy of the old one. |
| Spin rate | ✅ **halved 2026-07-19** (Jesse: "spinning slightly too fast"). Applied to the base rates inside `Spin`, not to the cfg defaults — a saved cfg or a preset setting `orbit:1` must also get the calmer rate. A grand house should turn like a slow orrery. |
| Focus = torch (dim non-neighbourhood) | ✅ |
| Focus = DoF/bokeh rack | 🔴 |
| Camera dolly-to-node on select | 🔴 |

### A3 · Motion (CRAFT §3)

| Commitment | State |
|---|---|
| Entrance: discs → spines → field, skippable, reduced-motion | ✅ |
| Flags ignite **last** so the eye lands on the problem | 🔴 depends on flags existing |
| **The 30-second trace (Act 2)** | 🔴 data present, **no choreography, no trigger, no UI** |

### A4 · Chrome (CRAFT §4)

| Commitment | State |
|---|---|
| Dossier: verb-grouped relations | ✅ |
| Concierge palette — find mode | ✅ |
| Palette — **ask mode** (routes to `/mcp`) | 🔴 |
| **Four view presets** instead of fourteen sliders | ✅ **shipped 2026-07-19** — OVERVIEW · DEPARTMENTS · LEDGER · TRACE as a top-center button bar, each a real `Partial<BoobooCfg>` (layer isolation + orbit/spines/fog/peel tuned per intent, not just cosmetic). The fourteen-slider drawer still exists — relabelled "pro controls", collapsed by default — for anyone who wants it, but it is no longer the front door. Verified live: `preset-departments.png` (ledger dimmed, spines lit), `preset-ledger.png` (structure dimmed, field forward), `preset-trace.png` (everything on, orbit/drift frozen for reading). |
| Dossier as **one shared component** (viewer + panel + site) | 🔴 two separate implementations |
| Loading / error / empty states | 🔴 |

### A5 · Organigram (CRAFT §5) — the craft pass, shipped 2026-07-19

| Commitment | State | Reality |
|---|---|---|
| House tokens applied | ✅ | |
| Department columns in cosmos sector order | ✅ | |
| Brass rails, orthogonal elbows | ✅ | Every `.oc-down` connector (root→lanes, and the department→staff rail inside each lane) now renders as a real brass rail (`--brass-line` gradient, glow on hover) instead of the plain grey/teal tree line; the lane rail is an explicit CSS-grid column (`grid-template-areas: "head rail staff"`) with a vertical spine + one orthogonal branch into the staff block — the elbow HANDOFF.md flagged as hidden inside lanes. Verified live: `chart-1.png` (headless GPU, 1600×1000, `/chart/`) shows the gold rail with rivet/fold glyph between each department head and its staff. |
| **The PLATE** (the card itself, not the rail it hangs on) | ✅ **rebuilt 2026-07-19** | Direction set by Jesse from two reference organigrams (a master-key hierarchy + a ministry chart) combined with the approved FHQ hero designs. The card is now a plate: 4px radius (was 15px — CRAFT §0 says 2/3/6), milled brass left edge whose opacity **dulls with rank**, left-aligned name-plate layout, engraved uppercase name, mono fact row. **The hash-hue rainbow is gone** — rank reads through brass finish, never hue, so the board finally agrees with the cosmos's gold/silver/bronze ladder. Emoji removed from all 51 staff plates (kept on GM + heads only). Vendor marks moved to the hero's white-chip treatment. Palette (wine ground + brass family) added to `design/tokens.json` so it is single-source, not a second hardcoded theme. Verified live: `real-board-3.png`. |
| **The left-to-right CASCADE** (master-key structure) | ✅ **built 2026-07-19** | Four real rank columns — **I** The Standard · **II** General · **III** Departments · **IV** Staff & machines — via two nested grids, with 61 orthogonal brass elbows drawn on a measured SVG plane (`CascadeRails`). Rank I is the House Standard rendered from the root agent's own declared `rules`, which is honest to the data: the thing above the GM is the law the GM alone may amend. Measurement uses `offsetLeft/offsetTop`, **not** `getBoundingClientRect`, because the chart sits inside a `transform: scale()` that rect maths would bake in. Verified live: `cascade-4.png`; rail count probed in the live DOM = 61 = 1 (law→GM) + 9 (GM→depts) + 51 (dept→staff). |
| Rank headers named, not inferred (ministry grammar) | ✅ | Four headers over the four columns they actually name, widths mirroring `.cascade`. |
| Card content: persona · health chip · bucket chips · rule count · last report | ✅ | `AgentFacts` (Panel.tsx) reads the SAME `orgBootSlice`/`pulseFor`/`lightFor` the dossier already used, so the card and its dossier can never disagree. Staff cards no longer print "Lift Engineer — Engineering" under "Lift Engineer" (`generate.mjs` staff no longer sets a duplicate `role`); every card face now shows a health dot, bucket-reach count, rule count, and last-report relative time. Verified live in `chart-1.png`: staff cards read "● quiet ▤3 §2 —"; department heads keep their persona line (unchanged, not duplicated) plus the same fact row. |
| **"Show the law" inheritance overlay** | ✅ | A top-bar toggle (`⚖ show the law`) adds `.law-on`: every rail in the tree (root→lane and the in-lane department→staff rail) switches from a static gradient to an animated gold dashed flow (`repeating-linear-gradient` + `background-position` keyframes, direction-correct on both the vertical and the horizontal branch), and every visible card grows a `.ag-law` line printing its actual boot-order chain (`slice.chain`, e.g. "General Manager ↓ Engineering ↓ Lift Engineer"). This is the real inheritance data, not decoration. Verified live via a CDP-driven click + screenshot (`law-on.png`): rails render as gold dashes, cards show the chain text, the hint line changes to explain what's on screen. `prefers-reduced-motion` freezes the flow animation. |
| Ledger shelf (hover a role → its reach lights) | ✅ | A persistent shelf docked under the chart lists every bucket the org declares (11, name-sorted) plus a permanently-locked `guest-registry` entry (the real sealed bucket id from `generate.mjs`/DESIGN.md — never fabricated). Hovering any card (`onMouseEnter`/`onMouseLeave` → `hoverId` → `orgBootSlice(...).buckets`) lights the matching shelf slots gold; the sealed slot is hard-coded to never light. Verified live via CDP hover + screenshot (`ledger-hover.png`): hovering Engineering's head card lit exactly `engineering`, `executive`, `house` — its real inherited reach. |
| Semantic zoom (house → department → role) | ✅ | Lightweight but real: each department's rail is itself the fold control (click or Enter/Space) — folded state replaces the staff row with a "N staff · folded to department level" summary and the department head stays visible (department rung); a house-level toggle in the zoom control (`⌂ house` / `▾ roles`) folds or unfolds every department at once. Not full LOD (no re-render of node detail at distance) but a genuine three-rung drill: house (GM only) → department (heads, folded) → role (every card). Verified live via CDP click + screenshot (`fold-house.png`): all lanes collapsed to head + summary chip, the toggle read "⌂ house". |

### A6 · Micro-brand + site (CRAFT §6/§7)

| Commitment | State |
|---|---|
| Copy rewritten to the agent-OS story | ✅ |
| Pemberton crest / "Est. 1927" / white-label proof | 🔴 |
| **Scrollytelling** (scroll chapters drive camera presets) | 🔴 static sections |
| Mobile: rendered video loop | 🔴 |
| OG image | 🔴 |
| Analytics events | 🔴 |

### A7 · Governance (CRAFT §9)

| Commitment | State |
|---|---|
| Golden frames committed | 🟡 one, of five |
| CI pixel-diff | 🔴 |
| Per-PR design QA checklist | 🔴 |

---

## B · The visitor journey (the conversion path)

The honest test: a stranger arrives knowing nothing. Can they understand the
product, play with it, and want it — without installing?

| # | Step | State | Reality |
|---|---|---|---|
| A1 | Land, grasp the pitch | ✅ | Hero + live brain behind it. Copy and scene agree. |
| A2 | See the brain | ✅ | `/viewer/` renders the Pemberton. |
| A3 | **Know what they're looking at** | ✅ | First-run orientation card names the house and its four bands top-to-bottom ("top band" / "the floor"), dismissible, recallable via "what am I looking at?". Verified live. |
| A4 | **Play, guided** | 🟡 | The card now hands over the real problem — "Start here · 5 flags → Water leak, Room 407" — one click into the dossier, and surfaces the palette. Still missing: view presets instead of 14 sliders, d-pad walk. |
| A5 | See the organigram | ✅ | Section 02 gives it a first-class card with its own pitch and CTA, alongside SEE and ASK. |
| A6 | **Understand the model** | ✅ | Section 03 teaches the five nouns — layer, agent, bucket, rule, boot — each with the Pemberton as the worked example, closing on a real unedited `booboo_boot("housekeeping")` slice showing rules in inheritance order. |
| A7 | Try the ASK face | ✅ | Four question chips fire real JSON-RPC at `/mcp` from the page and render the answer. Verified in-browser on the live domain: 3 majors this week · 98/14/12 absence ranking · incidents by department · the Housekeeping boot slice with rules in inheritance order. |
| A8 | Grasp "three faces" | ✅ | Section 02 names SEE / GOVERN / ASK, one card each, every one a live link. |
| A9 | Convert | 🟡 | `npx` command + GitHub. No "hosted / done-for-you" path, no email capture, nothing for a non-technical buyer. |

A3, A5, A6, A8 closed; A4 partly. The label is written: the site teaches the
model, the viewer names its own bands and points at the problem. What remains
of the wound is depth of play, not comprehension.

---

## B · Surfaces

| Surface | State | Evidence / what's missing |
|---|---|---|
| Landing `/` | ✅ | Six sections: idea · three faces · the model (five nouns + a real boot slice) · live ask · run-it · what-ships. Missing only OG image and scrollytelling. |
| Viewer `/viewer/` | 🟡 | Layout, floors, landmarks, torch, entrance, dossier-by-verb, palette, flags, verb colours, orientation card, **light-shaft spines**, **four view presets** all ✅ verified 2026-07-19. Missing: focus-camera dolly, d-pad walk, breadcrumb, weak-GPU guard. |
| Staff board `/chart/` | ✅ | Lanes stack downward, scrolls correctly, 15 real vendor marks, 3D hover, personas + rule counts + boot-order rules + contracts, read-only apply ✅. The CRAFT §5 craft pass shipped 2026-07-19 — see A5: brass rails + elbows, card faces (health/reach/rules/last-report, redundant role line gone), show-the-law overlay, ledger shelf, semantic zoom. All five verified live (screenshots below). Still open: golden-frame CI (A7), mobile pass (C9). |
| ASK `/mcp` | ✅ | Authless Streamable-HTTP, 8 tools incl. `booboo_count`, verified live from a real claude.ai connector: 3 majors this week, 98-vs-14 absences, boot slice correct. |
| Brand domain | ✅ | `booboo.fractionalhq.uk` — A record via Porkbun API, propagated, cert valid, all routes 200. |
| README | 🟡 | Leads with the zero-config command; badges; demo link. Not yet updated for `booboo_count` or the brand URL. |

---

## C · Product gaps found by using it

| # | Gap | State | Note |
|---|---|---|---|
| C1 | No aggregation | ✅ fixed | `booboo_count` — found by testing the live connector. |
| C2 | Panel counters read 0 | 🟡 **half-fixed — see C33** | type `memory`→`observation` translation… added to the demo site's serverless adapter **only**, never to the shipped `@booboo-brain/serve`. The ✅ was true of the deploy I checked and false of the product. |
| C3 | iframe canvas sizing race | ✅ fixed | Affected the shipped panel too. |
| C4 | Orbit swing emptied embeds | ✅ fixed | `orbit:0` for `?chrome=0`. |
| C5 | 3D labels bled through dossier | ✅ fixed | drei `zIndexRange` capped. |
| C6 | `panel-css.ts` had no generator | ✅ fixed | Claimed GENERATED, drifted silently. `scripts/sync-css.mjs`. |
| C7 | Weak-GPU/WebGL fallback | 🟡 | Guarded at the landing only. The **viewer package itself** still has none — a stranger deep-linking `/viewer/` on a weak device is unprotected. |
| C8 | No OG image | 🔴 | Every social share of the demo renders blank. Directly undercuts the distribution push. |
| C9 | Mobile | 🔴 | Landing degrades to a CSS starfield. `/viewer/` and `/chart/` on a phone: **unverified**, likely poor. |
| C10 | Golden-frame CI | 🔴 | One frame committed, no diffing. Design can regress silently. |
| C11 | Scale claim unproven | 🟡 | The million-node path has never been rendered end-to-end by me — only asserted. |
| C12 | **Dossier read the wrong dictionary** | ✅ fixed | Found by Jesse clicking Engineering and getting nothing but weight/tier/cluster/parent. The viewer's dossier was ported from the Dionisos OS and read `status_pill`/`p0_count`/`phase`/`last_move`/`next_plan`; the Pemberton emits `desc`/`health`/`note`/`lastBoot`/`role`. **Zero overlap** — so every agent in a 2,414-node demo rendered as raw graph metadata. Now reads the Booboo convention (`health`, `desc`, `note`, `lastBoot`) with the legacy keys as fallback, and the Overview shows persona · health **with its cause** · chain of command · reports filed · reach (buckets + rules) · last boot. Verified live on `agent:engineering`: `dossier-eng2.png`. |
| C13 | **No reports existed at all** | ✅ fixed | Jesse: "I click an alarm and I don't see reports at all." Correct — the snapshot contained **0 report nodes**. The 5-entry `journal.seed.jsonl` is a separate file replayed into a live server; the static viewer never sees it. A house "running for years" had no record of ever closing anything. Now generates **425 reports across 3 years** (9 departments monthly ×36, the GM monthly ×36, the Night Audit nightly ×60, 5 staff closes on the live incident). Node count 2,414 → 2,839. |
| C14 | **"Who is this report sent to?"** | ✅ fixed | Jesse's question, and the graph had no answer. Every report now carries `data.to` — staff → their head, head → the GM, GM → the House Standard — asserted at generation time to name a node that **exists** (a dangling recipient renders worse than none). The dossier prints `→ GM` on every report row and a `from → to` pair on a report's own dossier, both click-through. |
| C19 | **Light is now the default; "design for everyone"** | ✅ shipped | Jesse, 2026-07-19: *"a white background, very light bubbles, beautiful refined display."* A whole hotel uses this, not a room of developers — a stranger should not land in a terminal. Warm-white ground (never flat `#fff`), bubbles at 11–14px radius carrying depth in a three-step shadow set rather than borders, brass demoted from material to ~5% accent. Dark survives intact behind the toggle, `[data-theme="dark"]`. Agreed by mockup first (`light-mockup.png`) before any code — the same confirm-then-build loop that worked for the cascade. Verified live: `light-real-1.png`. |
| C20 | **Health light + motion** | ✅ shipped | A real lamp: lit core, soft wash ring, and an expanding halo that breathes every 2.6s — **offset per bubble** off a deterministic hash so nine departments never blink in unison (a synchronised board reads as a screensaver; a staggered one reads as a building with people in it). A failing lamp beats at 1.2s: urgency in the rhythm, not only the hue. Motion elsewhere is deliberately slight — bubbles rise 2px toward the cursor on one shared easing pair and settle on entry. All of it honours `prefers-reduced-motion`. |
| C21 | **The GM was modelled as a person** | ✅ shipped | Jesse: *"the GM cannot be a personne, it's a group of executives."* Correct, and a modelling error rather than a styling one. Rank II is now **The Executive** — a body with four seats (GM · Deputy GM · Director of Finance · Director of People & Culture), `root: "executive"` in the org, `agent:executive` in the snapshot, seats carried on `data.seats`. The generator's own dangling-reference assertion caught the journal still pointing at `agent:gm` during the rename. |
| C22 | **The Executive's dossier read 0 reports** | ✅ fixed | Worse than logged: house-level filers carry `cluster: null`, and the reports timeline resolved names by cluster — so the single most important entry in the house, *"Amended the House Standard § 14"*, rendered as **"unknown"**. Every report also carries `data.agent`; a `reportAgentId()` helper now prefers cluster and falls back to it, applied in the health map, the timeline, the filter chips and the dossier (which falls back to a full pull when the cluster query is empty). Verified live: 0 unknown filers, first filer reads "The Executive", filter chips include it, dossier reads 36 filed. |
| C23 | **The light pivot silently broke dark** | ✅ fixed | Found only because I went back and actually toggled. Lanes rendered as 50%-white slabs and rank I/II kept a light gradient on the dark ground — my `[data-theme="dark"]` overrides referenced `.bub` and `.lane-tray`, **class names from the mockup that do not exist in the panel**, so they never matched anything. Both surfaces are now themed vars (`--surface-tray`, `--surface-lift`) that flip properly. The lesson is narrower than "test both themes": an override that silently matches nothing looks exactly like an override that works. |
| C25 | **"If I move a link I cannot unlink it"** | ✅ fixed | Jesse's report, and it was two defects wearing one coat. Re-parenting itself worked (proven by driving real `DragEvent`s both directions), so the felt-stuckness came from (a) **only `.ag` cards accepted a drop** — release over the empty space inside a lane and nothing happened, silently, which reads as refusal; and (b) **the only undo was a global `discard`**, so correcting one mis-drop threw away every other edit and the rational move became to stop dragging. Now the whole lane is a drop target that lights up on hover (`Lane` in `Panel.tsx`), and `changes` returns structured `{key,id,kind,label}` objects so every pending row is its own undo button. `revertOne` restores parent-only for a move and everything-but-parent for an edit, so undoing one never silently undoes the other. Verified live on the deploy: dropped HVAC on Front Office's empty lane space (lane highlighted, parent `engineering → front-office`), moved CCTV to F&B, then undid **only** HVAC — it returned to `engineering`, CCTV stayed at `f-and-b`, and the pending bar went 2 → 1. This closes the interaction half of **G2**. |
| C26 | **The house and the executive held no memory at all** | ✅ fixed | Caught on the Executive's own dossier reading **"0 memories in reach"** — the root of a three-year-old hotel, while Engineering alone held 252. The 100 observations *were* generated and parented to `bucket:house` / `bucket:executive`, but stamped `cluster: null`, because the loop derived cluster from `DEPTS` and neither is a department. The dossier and the ledger shelf both count **by cluster**, so they were invisible. It never looked like a data bug; it looked like an empty company — which is exactly what Jesse flagged weeks ago as *"the cortex/atlas levels are empty, weird for a years old company."* Two causes underneath: (a) the generator **forbade** it — the assertion read `clusters.size !== 9` when the viewer's even-ring branch actually needs `<= RING_MAX` (16), so the 9 was over-tight and was itself what kept the house level unclustered; (b) the content was department-shaped, and the board does not take meter readings, so house/executive now carry their own register (standard revisions · licence renewals · insurance reviews · board minutes · capex approvals · sign-offs). New assertion: every declared bucket must cluster something **except** `guest-registry`, and it fails in the other direction too if the sealed bucket ever starts holding serialised observations, so the privacy wall is checked both ways. Verified live: house 70 · executive 30 · guest-registry 0 (still sealed) · engineering 252; the dossier reads **100**, was 0. |
| C27 | **Dossier: crushed role, banned side-stripes, an invisible texture** | ✅ fixed | Four defects found by actually looking at a crop of the card C26 had just fixed. (a) The role sat *inside* the header flex row, so the action buttons claimed width first and The Executive's four seats wrapped into a **7-line ribbon** down one side; it now breaks out to full panel width and reads in 2 (measured live across three dossiers of different role lengths). (b) `.doss-rep` and `.rule-card` both carried a 2px brass `border-left` — a **match-and-refuse banned pattern** in the house design laws, and pure decoration here since status is already carried properly by `.rep-dot`. Deleted rather than restyled. (c) The report hover was `rgba(255,255,255,.03)`, a dark-theme assumption that lightens nothing on a light board. (d) The ground-plane dot grid was white at 3.5% **on a near-white surface** — present in the stylesheet, absent on screen; now a themed `--dot`. (c) and (d) are both the C23 lesson again: a rule that resolves to nothing looks exactly like one that works. Verified in **both** themes on the deploy: `border-left-width` 1px and the theme's own line colour in each, `--dot` flips 27/24/21 ↔ white, role 2 lines. |
| C28 | **Six of nine departments were amber against a number nobody chose** | ✅ fixed | G3 depth, and the fix was to build *nothing*. `cadence` (expected hours between reports) already existed and `lightFor` already read it for **any** agent — but the spec documented it as "automations: expected hours between runs", so every reader believed it was machines-only and not one org unit declared it. All nine departments fell through to the panel's hardcoded `26`, a machine's rhythm applied to org units that report weekly, which left **6 of 9 amber permanently**. A board where everything is amber cannot show you the one thing that is wrong — the exact opposite of the health lamp's job. Fixed by declaring the real rhythms (front of house daily · back of house weekly · the table monthly) and correcting the spec so the field reads as general, with the fallback named as *a hidden decision, not a default*. **6 amber → 2**, and the two that remain are Housekeeping and Engineering, both amber because they filed a `warn` over the Room 407 leak. The card also now names the beat next to the age — "reported 4d ago · weekly" — because "reported 4d ago" alone is an oracle: the lamp knows whether that is fine and the reader does not. A build assertion fails if any org unit omits a cadence. Verified live: rendered tally `{Healthy: 7, Needs a look: 2}` across the nine department cards. |
| C29 | **`relTime` implemented twice, and the two had drifted** | ✅ fixed | Found by the don't-duplicate sweep, not by symptom. `relTime` existed in both `Panel.tsx` and `BoobooView.tsx`, and the copies had diverged into **two different answers for one fact, on two surfaces one click apart** (the dossier's `◉ 3D` button crosses between them): a 30-hour-old report read *"yesterday"* on the staff board and *"30h ago"* in the cosmos, and with Pemberton's three years of history the board printed *"1095d ago"* where the viewer printed *"3.0y ago"*. The viewer's version was strictly better (rounds instead of flooring, handles months/years, no "yesterday" special case that hides the hour count). Consolidated into `@booboo-brain/spec` — the package both already depend on, and which already exports behaviour (`validate` · `validateOrg` · `orgBootSlice`), so the export is additive and non-breaking — and both local copies deleted. Verified live: the board's dossier now reads "2mo ago" / "3mo ago" where it previously read "67d ago" / "96d ago". Also deprecated `BOrgAgent.reports?: string` in the same pass: declared as "ref to its report stream", read by **nothing**, and a second way to say what the journal already answers by agent id. |
| C30 | **The OS shows a two-week-old panel — version drift, not a missing mount** | ✅ shipped | Investigating "Dionisos OS should display the proper booboo" found the mount already built and correct: `OrganigramPage.tsx` renders `@booboo-brain/panel` from npm (never a fork) and `/api/booboo/[fn]` + `/api/org` adapt the live Dionisos brain to the panel's routes. The GOALS doc's claim that the OS "renders something else" was **wrong** and is corrected. The real gap: the OS is pinned to `panel ^0.5.2` (installed 0.5.2), wired 2026-07-04, *before the entire craft pass* — and the packages were never version-bumped despite dozens of commits, so npm still serves the old code. **Proven** the current panel renders the real 79-agent fleet by running `booboo panel` against the live `dionisos.booboo.json` (79 agents · 10,237 nodes · 6,997 memories · the real cron fleet as automation cards) — screenshot banked; it is excellent. Closing it is a **release**, Jesse-gated on two boundaries: (1) publish spec + panel + viewer + create-booboo together (panel/viewer now import `relTime` from spec, so spec must ship with them); (2) `npm update` in the OS + redeploy (Dionisos production). Everything up to those two gates is done. **Both gates cleared 2026-07-19 (Jesse-authorised):** published `spec 0.3.1 · panel 0.5.4 · viewer 0.1.2 · create-booboo 0.5.0` (`workspace:*` resolved to `spec@0.3.1` in panel/viewer — verified on npm); merged `booboo-panel-cadence`, `npm update`d the OS to panel 0.5.4, deployed to `dionisos-graph.vercel.app`. **Live-verified in the real browser:** the OS `booboo` tab now renders the current panel over the real 93-agent fleet — source cards, the rank cascade, health lamps, the redesigned dossier. G4's mechanism proven end-to-end: main → publish → npm → OS → deploy → render. |
| C31 | **The real Dionisos fleet would render all-amber** | ✅ shipped | Same class as C28, on the actual customer. The OS's `/api/org` set **no cadence** on any agent, so all 79 cards inherited the panel's ~26h fallback — a machine's rhythm on org units that report on human time. Project reports do bind (org ids and report clusters both carry the `proj:` prefix — verified), so the 9 projects would sit permanently amber, and an all-amber board cannot show the one thing wrong. Fixed on `dionisos-graph@booboo-panel-cadence`: org units get a declared review rhythm (portfolio monthly · project weekly · cron-family daily), and each machine is judged against its own schedule via `scheduleToCadence(cron_expr)`. The parser was **verified against all ~60 live cron expressions** — every one parses to a sane value, erring lenient so a card never false-ambers (hourly→1 · every-Nh→N · daily→24 · weekly→168 · monthly→720). tsc clean. **Live-verified in the real browser after deploy:** `proj:fractional-hq` and `proj:tasknext` read "· weekly", cron families "· daily", the portfolio root "· monthly"; the fleet's lamp distribution is 14 none · 9 ok · 2 warn — **only 2 amber, both genuine heartbeat-driven**, where the 26h fallback would have ambered nearly all of them. |
| C32 | **The OS board rendered split-brain — a host's shell was shadowing the panel's tokens** | ✅ fixed | Jesse: the OS `booboo` tab is "neither cleanly light nor cleanly dark, it just looks broken". The document root resolved `data-theme=light` and `body` went warm-white, while the cards rendered `#0d121a`. **The handoff's three suspects were all wrong** (dark wrapper · OS managing the root · CSS load order) and its "quick fix" — write `localStorage['booboo-theme-v2']='light'` — would have changed **nothing**, because the key was already null and the panel was already resolving light. The real cause is a **custom-property collision**: `dionisos-graph`'s `globals.css` declares 11 of the panel's own token names on `.os-root` (`--bg`, `--bg-1`, `--bg-2`, `--line`, `--line-2`, `--gold`, `--amber`, `--warn`, `--violet`, `--ink-2`, `--ink-3`). `.os-root` is an **ancestor** of the panel and custom properties inherit, so its dark values won inside the board no matter what `data-theme` said — specificity and stylesheet order never entered into it. Measured on the live OS: `--bg-1` was `#fffefb` at `<html>`, `#0d121a` at `.os-root`, and `#0d121a` at `.pnl`. A second bug of the same family sat next to it: the panel's `body { background: var(--bg) }` was **repainting the host's document** (that white body), and `useTheme` was writing `data-theme` onto the host's `<html>` — a mounted component reaching out and restyling the app around it, and obeying any host that set `data-theme` for its own reasons. **Fixed at the root:** the panel's tokens are declared on `.pnl, .pnl-fatal` instead of `:root` (`scripts/gen-tokens.mjs` now emits `:root` for the site and the panel's own roots for the panel), `data-theme` moved onto those same elements, the dark rules scoped to `.pnl[data-theme="dark"]`, and the `body` rules deleted. Nothing can now sit between the token names and the elements that read them. Added a `theme?: "dark"\|"light"` prop so a host can **pin** the palette — previously impossible, since `localStorage` belongs to the visitor and `?embed` belongs to the iframe. **Verified against a real host reproducing the collision** (`booboo panel` over the Pemberton, with `body` made to declare all 11 of Dionisos OS's values): `.ag` stayed `rgb(255,254,251)` and `--bg-1` at `.pnl` stayed `#fffefb` — **unchanged** where the same declaration previously repainted the board. Toggled to dark under the same collision: cards `rgb(36,22,36)`, `--ink` `#f4eee2`, correct — and `<html>` stayed `light` throughout, proving the panel no longer writes to its host. Both themes screenshotted. `tsc` exit 0, panel build clean, `web/dist` rebuild clean with `web/tokens.css` still correctly on `:root`. **Design call (Jesse, 2026-07-20): the OS pins `theme="dark"`** — the cockpit is dark, the panel ships a full verified dark theme, and a white slab in a graphite room reads worse than either; the demo stays light for strangers. **Not yet live:** the OS change needs `panel >= 0.5.5`, so it is behind the npm-publish and Dionisos-deploy gates (version staged at 0.5.5, unpublished). |
| C33 | **`booboo panel` reported 0 memories over 2,100 of them — C2 was fixed in one of two code paths** | ✅ fixed | Caught while verifying C32: the local CLI panel over the Pemberton read "0 memories" in the header and "0 memories in reach" on every dossier, where GAPS C26 claims 100 verified live. Both were true. The panel asks `/nodes?type=memory`; the Pemberton's ledger entries are `type=observation`. The alias existed — in `web/api/panelapi.mjs`, the **demo site's own serverless adapter**, and nowhere else. `booboo panel`, the command every OSS user actually runs, goes through `@booboo-brain/serve`, which passed the type straight through. So C2's ✅ was true of the surface I had checked (the deploy) and false of the shipped product, and C26's verification inherited the same blind spot: the data was always correct, the CLI serving path was hiding it. Proven by curl before the fix — `type=memory` → `{"total":0}`, `type=observation` → `{"total":2100}`. **Fixed in the index** (`packages/serve/src/graph.ts`), not at the callers: a `TYPE_ALSO` widening applied inside `clusters()`, `list()` and `count()`, so REST, MCP and the demo adapter can never answer differently again. **The first attempt was wrong and CI caught it**: I wrote the alias as a *rename* (`memory`→`observation`), which broke the existing graph test — correctly, because `JournalWriter` writes remembered notes with `type: "memory"` literally, so a rename would have made every journal-written memory unqueryable. It is a **widening**: `type=memory` matches both conventions, `type=observation` still selects only itself. A regression test now pins both directions, because the first fix widened one and broke the other — and the duplicated translation in `panelapi.mjs` was deleted, leaving one implementation. Same shape as C29's duplicated `relTime`: two implementations of one fact, only one of them fixed. Verified by curl after: `type=memory` → `{"total":2100}`, and `clusters?type=memory` → `house: 70 · executive: 30 · engineering: 252` — **exactly C26's numbers**, with 70+30 = the 100 its dossier claim cites. tsc 0, serve build clean, 16/16 serve tests pass. **UI not re-read**: the browser window was hidden, which throttles rAF and freezes every `useCountUp` at 0 (C17's landmine — "9 direct reports", un-animated, rendered fine in the same read). curl is the correct rung for a serving defect; the on-screen confirmation is still owed. **And there was a THIRD copy.** `web/api/_index.mjs` is a hand-vendored `BoobooIndex` (deliberate — the demo's serverless functions must not drag in express + fs/journal, nor wait on an npm publish) whose header says *"if graph.ts changes, re-copy them here"* — a rule with nothing enforcing it, which is the actual reason C2's fix landed in `panelapi.mjs` alone. Re-synced to serve@0.4.1, and **wired a drift guard into CI** (`scripts/check-vendored-index.mjs`): it runs 8 probes against both indexes over a graph holding one `memory` node and one `observation` node and fails if they disagree, plus an absolute assertion so a regression in *both* copies still fails rather than comparing equal. **Guard proven to work by breaking it on purpose** — reverting the vendored alias produced `list type=memory: vendored=1 packaged=2` and exit 1; restored, it passes. **The lesson, and it is the C2 lesson generalised:** verifying on the deploy does not verify the product when the deploy runs its own copy. Check the path the USER runs — and when a comment says two files must agree, make CI say it instead. |
| C18 | **Cascade buried its own master key** | ✅ fixed | Two layout defects caught by measuring the live DOM, not by eye. (a) The root grid had rows `145px / 2096px`, so the GM pinned to the top of row 1 while the lanes spanned both — plate and rack now share one cell (`.casc-head`). (b) With the lanes column at `1fr` under `.chart { min-width: max-content }`, staff never wrapped: 2,400px natural width, fit-to-width scaled it to **52%** and every plate became unreadable. Bounding the column to `minmax(420px, 940px)` wraps rank IV and restores **77%**. Then centring — correct for a 3-branch key chart — buried the Standard and the GM below the fold at nine departments, so the cascade top-aligns. |
| C16 | **Panel shipped a palette that resolved to nothing** | ✅ fixed | `app/index.html` never linked `tokens.css`, and the panel injects its styles as ONE `<style>` string so a host needs no import. The old `panel.css` hardcoded hex values, so nobody noticed. The moment it started referencing `var(--wine)`/`var(--brass)`, every colour resolved to nothing and the board rendered as a grey wash. `sync-css.mjs` now concatenates `tokens.css + panel.css` into `PANEL_CSS`, **and fails the build** if `panel.css` consumes any custom property that nothing defines (vars with fallbacks and the two set inline are exempt). It caught 4 on its first run, including a stale `--brass-lo`. |
| C17 | **Screenshots caught mid-animation read as data bugs** | ✅ fixed | A capture showed "61 agents · 325 nodes" against a 76 / 2,839 dataset and looked exactly like the API serving stale data — it was `useCountUp` still counting. The API was verified correct by curl. Captures now pass `--force-prefers-reduced-motion`, which the code already honours (`REDUCED` lands counters instantly). Second instance of the HANDOFF landmine "finish or cancel animations before trusting any read". |
| C15 | **Reports cited events that hadn't happened** | ✅ fixed | Introduced by C13 and caught three times on the live render, not in review: a monthly close dated 32d ago (then 3.2d) describing the Room 407 leak from 1.2d ago, then the GM's amendment dated before the leak it responds to, then a close 35d back narrating a repair made after it. Dates were being tuned by eye. Now a **causality assertion** in `generate.mjs` fails the build if any report mentioning the incident is dated before it. The chain reads correctly: incident 1.2d → attendant 1.05d → night engineer 0.55d → Engineering closes to GM 0.4d → GM amends the Standard 0.25d. |

---

## D · Process

| # | Gap | State | Fix |
|---|---|---|---|
| D1 | No done-vs-intended map | ✅ fixed | This file. |
| D2 | Follow-ups scattered | 🟡 | Consolidated here; commit messages must now reference a row. |
| D3 | Stale secrets doc outranked canonical | 🔴 | `.secrets/porkbun_credentials.md` still says the key was "LOST AT CREATION". It wasn't — `.env.master` has both. A stale doc cost a wrong "blocked" call. Correct the doc; canonical is `.env.master`. |
| D5 | **Headless-with-a-fresh-profile is not the user's browser** | ✅ rule | Every verification this project has done ran in headless Chrome with a clean `--user-data-dir`. That proves the BUILD works; it cannot prove the PRODUCT works, because a fresh profile has no localStorage, no cache and no session. The whole stale-theme defect (C24) lived in exactly that blind spot, and Jesse had to find it twice while every screenshot I sent looked correct. **Rule: anything touching persisted state, sessions, caches or "why does mine look different" gets checked in the real browser via `claude-in-chrome` BEFORE asking the user to look.** Confirmed the fix that way: his profile still holds the legacy `booboo-theme: "dark"`, `booboo-theme-v2` is null (he never chose it), and the embedded board reports `data-theme: light` regardless. **Re-confirmed 2026-07-19 in Jesse's real Chrome, and it is the gold case:** his profile still holds the legacy `booboo-theme: "dark"`, `booboo-theme-v2` is null (never re-chosen), and the demo renders `data-theme: light` anyway — the exact stale-state that broke it twice, now correct. Same session verified the drag fix there too: the lane lit on hover, two drops both registered as pending, and undoing only HVAC left CCTV (pending 2 → 1). D5 fully closed. |
| D6 | **The dark cosmos is load-bearing, not a coherence defect** | ✅ closed by argument | I had filed "cosmos is blue-black while the board is light" as a one-ordering-law violation and queued a light-cosmos pass. Reading the code before touching it killed the idea, correctly: the cosmos's whole visual hierarchy is **emissive luminance against darkness** — `Flags` is documented as "luminance rank 1, the brightest thing on screen… if a frame's brightest pixel is not one of these, the frame fails QA", and the sprite SDF, the spine shafts and the pulse-river edges all rank by brightness too. On a white ground nothing can out-brighten the background, so inverting it would destroy the exact mechanism that makes a million-node brain *show you the problem*. Two grounds is the correct read anyway: light = the working surface you edit, dark = the deep view you look into. **Do not reopen without first solving flag-finding on a light ground** — that, not the palette, is the blocker. |
| D4 | "Verified" used loosely | 🟡 | Rule: "verified" means an artefact was observed (screenshot, curl, live query) — not that code was written and built. |

---

## F · Build order

Ranked by *conversion impact per hour*, not by what's fun. Every item names
the row it closes.

**Tier 1 — the product currently fails its own acceptance test**

1. ~~**Flags + health rendering** (A1)~~ ✅ shipped + verified live.
2. ~~**Verb colours + direction on edges** (A2)~~ ✅ shipped + verified live.
3. ~~**Tokens actually consumed** (A0)~~ ✅ shipped + verified live.

**Tier 1 is closed.** All three were specified, assumed done, never built.

**Tier 2 — the label for the machine**

4. ~~**The model explainer** (A6)~~ ✅ shipped + verified.
5. ~~**Orientation in the viewer** (A3)~~ ✅ shipped + verified. Guided play
   (A4) partly — presets and d-pad still open.
6. ~~**Promote the organigram** (A5/A8)~~ ✅ shipped + verified.
7. ~~**In-page ask** (A7)~~ ✅ shipped + verified in-browser. Palette
   ask-mode (A4) still open — same engine, second surface.

**Tier 2 is closed.** The label is written.

**Tier 3 — the craft pass — REOPENED 2026-07-19 (Jesse: cards are "cheap")**

> I closed this the same day I opened it, on my own say-so, and Jesse's first
> look reopened it. The mechanics landed (data, rails, overlay, shelf, zoom,
> spines, presets); **the card material did not**, and the card is what a
> visitor actually looks at. Mechanics ≠ craft. Do not mark this closed again
> without Jesse's eyes on it — that is what "only GAPS.md may say done" is
> for, and I broke it once already.

8. **The craft pass** — the one that decides whether this is a category of
   one. See `design/HANDOFF.md`.
   - GOVERN (A5) — **shipped + verified live**: staff-card faces, engraved
     brass rails + elbows, show-the-law overlay, ledger shelf, semantic
     zoom. All five confirmed against the live site (three via a
     CDP-driven click/hover + screenshot, not just a static render).
   - SEE (A2, A4) — **shipped + verified live**: light-shaft spines (the
     signature element, specified since 2026-07-18, built and shipped
     2026-07-19) and four view presets replacing the slider drawer as the
     front door. A real rendering bug was found and fixed in the same slice
     — see the landmine in HANDOFF.md.

   Both faces now carry hospitality materiality that answers Jesse's
   verdict directly: the org chart is a brass staff board with a rule of
   law you can trace card-by-card; the cosmos has authority literally
   falling as light. Remaining Tier-3 proof items (below) are polish and
   CI, not the category-of-one question.
9. OG image (C8) · 10. mobile pass (C9) · 11. the 30-second trace (A3) ·
12. weak-GPU guard (C7) · 13. golden CI (C10) · 14. million-node proof (C11).

Tier 1 is why this file exists: three items that were specified, assumed
done, and never built. Tier 2 is the same insight as before — **we built the
machine and never wrote the label.**
